# KLUE-MRC autoresearch 결과 (132 run)

2026-09-19 ~ 09-20, 약 30.7시간, 후보 132회(keep 8, crash 1). **baseline 대비 실질적인 개선은 없었다.**

## 최종 수치

| | commit | search EM | ROUGE-W | 빈 응답 |
|---|---|---|---|---|
| baseline | `fb0e7a3` | 76.02 | 79.67 | 392 |
| 최고 기록 | `2d674bf` | 79.69 | 83.26 | 340 |

최고 설정: lr 2.5e-4 linear, warmup 3, adamw_8bit, clip 1.0, dropout 0.1, LoRA attention r16 + MLP r64.

## 이 +3.67을 개선으로 볼 수 없는 이유

결과가 바뀌지 않아야 하는 변경을 일부러 세 번 돌렸다. 모두 최고 설정에서 한 값만 건드렸다.

| 변경 | 실제 효과 | search EM |
|---|---|---|
| `max_grad_norm` 1.0 → 0.5 | 모든 step이 이미 clip됨, Adam은 스케일 불변 | 75.68 (-4.01) |
| `weight_decay` 0.001 → 0 | 전체 학습 동안 가중치 0.002% 차이 | 76.79 (-2.90) |
| `weight_decay` 0.001 → 0.0005 | 그 절반 | 74.74 (-4.95) |

수치 수준 섭동만으로 EM이 3~5점 움직인다. keep된 향상 폭은 +0.17 ~ +0.94로 전부 이 잡음 안이므로, 79.69는 설정의 실력이 아니라 잡음이 위로 튄 한 번의 값이다. 같은 설정의 기대값은 75~78로 보인다.

## 잡음보다 크게, 반복해서 나온 결과

1. **attention LoRA는 이 과제에 필요 없다.** 전부 빼도 78.84이고, rank를 32 이상으로 올리면 74점대로 떨어진다.
2. **그 손해는 업데이트 크기가 아니라 학습 파라미터 수에서 온다.** rank 32에서 scale만 0.5로 낮춰도 74.91로 동일하다.
3. **MLP LoRA는 32층 전부에 필요하다.** 상위 16층에만 붙이면 70.73(-8.1), eval loss도 0.242 → 0.321. 반면 attention은 상·하위 어느 절반에 붙이든 76.96으로 같다.
4. **MLP 세 모듈 중 `down_proj`가 핵심이다.** 빼면 74.40, 그것만 써도 77.39.
5. **학습 후반 lr이 0으로 끝나야 한다.** 하한을 10%로 두든 2.5%로 두든 빈 응답이 450개 안팎까지 늘어 과잉 기권이 된다. 하한의 크기는 무관하다.

## 다음에 이 과제를 이어받는다면

- 단일 run 비교로는 아무것도 판정할 수 없다. 설정 하나당 최소 3회를 돌려 평균으로 비교해야 한다.
- eval loss는 EM과 상관 -0.56으로 방향 지표로 쓸 만하지만, rank 8 차이 같은 미세 구간에서는 단조롭지 않다.
- 후보별 전체 수치는 `results.tsv`(132행), 가설과 판단 근거는 `summary.md`에 있다.
